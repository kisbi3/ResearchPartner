# Adoption Decision — researcher-owned

<!--
  WRITTEN ONLY BY THE RESEARCHER (PI). Agents (Lead, grad students, reviewers)
  are hook-blocked from writing it (scripts/path_check_hooks.py). This is the
  brake for brownfield onboarding: the lab inventories the existing project and
  proposes; you decide.

  This file exists ONLY when the harness is attached to research that is already
  in progress (task-intake category "Existing project onboarding"). On a fresh
  greenfield project it stays a blank stub and onboarding mode is inactive — the
  normal Orient → Interview → Model → Seed gate chain applies unchanged.

  The lab first records the inventory in:
    docs/adoption/existing_project_intake.md
    docs/adoption/existing_results_inventory.md
    docs/adoption/retrofit_validation_plan.md
  Read those, then sign your decision below.

  Effect of a signed decision (a non-empty "## Decision" section): the harness
  enters ADOPTION MODE. The Model gate and the Baseline-strategy gate become
  satisfied-by-adoption (the existing model and the chosen reproduction baseline
  are accepted because YOU accepted them here — not re-authored from scratch).
  The Baseline gate is NOT waived: a claim is validated only after the chosen
  result is actually reproduced and recorded in docs/gates/baseline_registry.md.
-->

## Decision

<!-- accept existing project / revise / reject — blank keeps onboarding mode inactive -->

## Accepted Existing Model

<!-- the implemented model you accept as the working model; point to its code/notes -->

## Reproduction Baseline

<!-- the existing result (one figure/table/run) the first retrofit must reproduce -->

## First Retrofit Target

<!-- the smallest target to bring under the harness first -->

## Per-Artifact Status

<!-- per adopted artifact: validated/partial/unknown/failed/waived/deprecated -->

## Date

<!-- YYYY-MM-DD -->
